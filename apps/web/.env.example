# Web app environment — only VITE_* vars are exposed to the client bundle.
# Do NOT put secrets here. Server-only env vars belong in apps/server/.env.local.

# Server proxy target port (defaults to 3001)
HAETAE_SERVER_PORT=3001
