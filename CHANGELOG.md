# Changelog

## v0.2.0 — 2026-09-06

앱을 영어로 열고, 두뇌를 프로젝트 경계 밖으로 넓히고, 비용 숫자를 믿을 수 있게 만든 릴리스.

### i18n — 앱이 영어로 열린다
- 앱 UI 전면 영문화 (#165). 사이드바·뷰·Settings 까지 크롬 전체가 한 언어로 일관 (#376, #378)
- `dayjs` 로케일 ko → en — 영문 UI 안에서 상대시간만 한국어로 새던 것 수정 (#380)
- 명세·ADR 은 `docs/` 에 한국어로 남는다 (단일 진실 공급원)

### 두뇌 — 프로젝트 하나에서 전부로
- **전역 cross-project 두뇌** — 모든 프로젝트의 노트를 한 인덱스(`project/slug`)로 묶어 의미 회상. 프로젝트 두뇌가 늘수록 값이 오르는 첫 배관 (#387)
- **토픽 페이지** — 개념 단위로 위키·노트를 가로지르는 합성 페이지 (#386)
- **외부 소스 흡수** — URL 을 떨구면 provenance 를 분리한 채 두뇌에 편입. 대화가 아닌 지식도 들어온다 (#391)
- **노트 근거 세션 인용** — 각 원자 노트가 자기가 나온 세션을 가리킨다. 노트 상한도 코퍼스 규모에 비례 (#389)
- 위키 스켈레톤에 아키텍처·트러블슈팅 섹션 + 합성 디테일 규칙 (#385)

### 소유권 — 볼트가 두뇌 전체를 담는다
- 옵시디언 export 에 온톨로지·노트↔개념 링크·토픽 페이지 포함 (#400)
- 자동 브레인 갱신 시 이미 export 된 볼트를 자동 재export — 두뇌와 볼트가 갈리지 않는다 (#402)

### 모델 · 비용 정확성
- **모델 목록을 CLI 티어 별칭으로** (`opus`/`sonnet`/`haiku`) — 핀된 id 를 세 곳에 복제하던 것을 정본 하나로. 세대가 바뀌어도 목록이 뒤처지지 않는다 (#404)
- **기본 모델 Sonnet** + fold 임계 상향 — 셀프호스트 사용자의 quota 소비 최소화 (#397)
- **Sonnet 5 단가 반영** ($2/$10). 패밀리 substring 매칭은 Sonnet 5 와 4.6 이 둘 다 현역이라 더는 성립하지 않아, 같은 단가로 청구되는 묶음 단위 티어로 교체. Fable/Mythos 도 `unknown`(=$0) 에서 실제 요율로 (#405)
- **`reprice`** — 인덱서는 비용을 인덱싱 시점에 굳히고 기존 행을 다시 보지 않는다. 단가 수정만으로는 과거 집계가 안 고쳐지므로, 저장된 토큰으로 제자리 재계산하는 도구를 추가. JSONL 재파싱·커서 리셋 없음, 기본 dry run (#405)

```
pnpm --filter haetae-server reprice            # 리포트만
pnpm --filter haetae-server reprice -- --apply # 기록
```

### 정리 · 수정
- 미사용 의존성 8개 제거 + dead code 삭제 + 사용량 뷰 크롬 공용화 (#393, #395)
- 위키 파괴 가드 — 증분 fold 가 위키를 붕괴시키면 커밋 거부 (#374)
- SYNC 버튼이 OAuth 5h/7d 사용 한도까지 강제 갱신하도록 배선 (#382)

## v0.1.0 — 2026-07-12

첫 공개 릴리스. Claude Code 를 위한 로컬 전용 개인 콘솔 + "AI를 위한 두 번째 뇌".

### Watching — 사용량 가시화
- `~/.claude` JSONL 자동 인덱싱 (부팅 + 30초 증분, 정수 micro-USD 정밀도)
- Overview 대시보드 — 롤링 윈도우(5h/24h/7d/월), 30일 추이, 캐시 인사이트, 히트맵
- Anthropic Admin API 미러 + Local vs API 비교 (Admin 키 선택)
- 전 프로젝트 세션 목록(LIVE 배지) + 메시지 타임라인 drill-down
- 실제 5h/7d 한도 % (opt-in, macOS Keychain / Linux·Windows credentials 파일)

### Second Brain — 자기개선 메모리
- 대화 → 프로젝트 위키 증분 정합 합성 (임베딩 없이 인덱스 + `claude --print`)
- 위키 → 원자 노트(제텔카스텐) · 타입 온톨로지 · 노트↔개념 링크 파생
- eval 자가감사(0–100 신뢰점수) → 위키 재생성 피드백 (자기교정 루프)
- 위키를 `.claude/CLAUDE.md` 에 2-tier 주입 — 다음 세션이 히스토리를 알고 시작
- MCP 회상 서버 (`recall_notes` · `recall_global` · `ask_brain`)
- 옵시디언 마크다운 볼트 export

### 통합 두뇌 뷰
- Knowledge — 전 프로젝트 위키·노트·개념 카탈로그 + 대화 전체검색(FTS5 trigram) + Ask(전역 의미 회상 / 프로젝트 Q&A)
- 그래프 — 프로젝트·노트·개념을 옵시디언식 라이브 그래프로 (레이어 토글, 노드 상세 패널)

### Guarding / Working / Research
- 룰·스킬·CLAUDE.md 관리 (화이트리스트 FS + 덮어쓰기 전 백업, Monaco 편집, diff)
- 통합 터미널 (node-pty, 멀티 탭, dock 영속, cwd 화이트리스트)
- NotebookLM 사이드카 (FastAPI) — 노트북 미러·Q&A·Settings 재인증

### 보안 원칙
- `127.0.0.1` 전용 바인딩, 단일 사용자, 시크릿은 서버 env 만 (클라이언트 번들 미포함)
